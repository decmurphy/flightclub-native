import { IfPlatformDirective } from './if-platform.directive';

describe('IfPlatformDirective', () => {
  it('should create an instance', () => {
    const directive = new IfPlatformDirective();
    expect(directive).toBeTruthy();
  });
});
